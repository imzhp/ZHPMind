# v2.5 Self-Review 报告

> 主对话于 2026-05-23 对 `.tmp-claude-reports/incoming-v2.5/` 全部 7 个文件做的严谨自审。
> 按 design-principles v2.5 §13.2「self-review 过渡方案」执行。
> 标注：[self-reviewed] by claude.ai main session

## 审查范围

7 个交付文件 + 跟 vault 当前 design-principles.md（v2.4-X, commit cbdb7b0）基线对比。

## 总评

🟢 **可部署**。0 个 🔴 阻塞，3 个 🟡 建议修订（非阻塞，可在部署前修也可部署后单独 patch）。

---

## 1. 完整性 ✅

所有 7 个文件结尾完整，无截断：

| 文件 | 结尾段 | 状态 |
|---|---|---|
| design-principles-v2.5.md | Changelog v1 条目 | ✅ |
| CLAUDE-v3.md | Changelog v1 条目 | ✅ |
| skill-concept-fable.md | 元注段 | ✅ |
| concept-fable-SKILL.md | References 段 | ✅ |
| concept-fable-references-prompt-template.md | 修订记录 v1.0 | ✅ |
| review-digest-v2-SKILL.md | v1→v2 演化说明 | ✅ |
| DEPLOY-v2.5.md | 出错回滚段 | ✅ |

## 2. 行数控制 ✅

按文件大小 ÷ 中文 markdown 平均 70-80 字节/行估算：

| 文件 | 大小 | 估算行数 | soft limit | 余量 |
|---|---|---|---|---|
| design-principles-v2.5.md | 32,904 B | ~440-470 行 | 600 | 安全 |
| CLAUDE-v3.md | 38,529 B | ~510-560 行 | 800 | 安全 |
| concept-fable-SKILL.md | 4,661 B | ~120 行 | 150 | 安全 |

🟡 **DEPLOY-v2.5「验证清单」预期偏高**：写的是"design-principles 约 560-600 行 / CLAUDE.md 约 700-770 行"，实际更短。**不阻塞**——Claude Code 看到实际行数显著低于预期时只是会困惑一下，不影响部署。

## 3. v2.4-X 基线完整保留 ✅

逐项核对 v2.1 / v2.2 / v2.3 / v2.4-X 的所有实质章节：

| v2.4-X 关键内容 | v2.5 中状态 |
|---|---|
| v2.1 Cross-modal eval 技术实现（第三层）| ✅ 完整保留（含 Hermes 多 profile 描述）|
| v2.2 Skill 系统设计 5 条原则 | ✅ 完整保留（经验沉淀/方法论与实现分离/来源分层/协议互通/维护机制分工）|
| v2.3 业务实体命名约定 | ✅ 完整保留（判断流程/别名机制/Tag 跟随/示例表）|
| v2.3 type 表扩展（含 moc + skill）| ✅ 完整保留 |
| v2.4-X snapshot 不三段式 | ✅ 完整保留 |
| v2.4-X 对话沉淀产物 sources:[] 合法化 | ✅ 完整保留 |
| v2.4-X aliases 三种用法（含迁移兼容）| ✅ 完整保留并显式化 |

**v2.5 主动删除的内容（合理）**：

- v2.4-X「对已有页面的影响」段（v2.3 的迁移待办清单）—— Phase D 已全部完成，v2.4 Changelog 已记录确认。v2.5 删除符合「删除优于保留」心法。✅
- v2.4-X 业务实体命名约定例子表的「亚马逊平台」第 4 行 —— 第 1 行 wildlume 已覆盖该原则，第 4 行冗余。v2.5 简化符合「减优于加」心法。✅
- v2.4-X Changelog 详细条目压缩为简版 —— 历史叙事归档到 git log，宪法不保留长篇变更日志。符合反膨胀原则。✅

**v2.5 主动修正 v2.4-X 不一致**：

- v2.4-X 第三层「导航」tag 例子用 `#业务/曜野`，但 v2.3 业务实体命名约定明确规定 tag 跟主 token、不用斜杠（`#wildlume`）—— **v2.4-X 自相矛盾**。v2.5 改为 `#wildlume` `#ecommerce` 例子。✅

**v2.5 补全 v2.4-X 漏掉**：

- v2.4-X 第三层物理结构图漏 `wiki/CLAUDE.md` 和 `wiki/log.md`（实际存在但未列）。v2.5 补全。✅

## 4. 跨文件内部一致性

### 4.1 type 表 ✅ 一致

design-principles v2.5「内容分类」表 = CLAUDE-v3 §3.1 表，10 个 type 一一对应。

### 4.2 soft limits ✅ 一致

| 对象 | design-principles | CLAUDE.md §12 Lint |
|---|---|---|
| design-principles.md | 600 | > 600 触发审 |
| wiki/CLAUDE.md | 800 | > 800 触发审 |
| wiki 单页 | 500 | > 500 应拆 |
| Timeline 段 | 30 条 | > 30 应归档 |
| MOC 页 | 200 | > 200 应分级 |
| inbox 单条 | 100 | > 100 应 distill |
| SKILL.md | 150 | > 150 应拆 |

### 4.3 propagation 四步 + 变更影响矩阵 ✅ 一致

design-principles 第二层「propagation 的具体执行」= CLAUDE-v3 §11，四步内容 + 6 行变更影响矩阵完全对齐。

### 4.4 AI 心法 + 红线 ✅ 一致

design-principles 第二层「AI 心法 + 红线」三心法 + 三红线 = CLAUDE-v3 §12.5（心法）+ §13（红线）。

---

## 5. D9 spec 自洽性 ✅

skill-concept-fable.md 完全符合 design-principles v2.5 §3.8 / CLAUDE-v3 §3.8 spec：

**Frontmatter 字段对照**：

| §3.8 spec 要求 | skill-concept-fable.md 实际 |
|---|---|
| title: {name}（不带 skill- 前缀）| ✅ `title: concept-fable` |
| type: skill | ✅ |
| status: active\|deprecated\|experimental | ✅ `status: active` |
| created/updated: YYYY-MM-DD | ✅ |
| tags: [skill, ...] | ✅ `[skill, concept-learning, distill]` |
| sources: [] | ✅ |
| source_count: 0 | ✅ |
| discussions: [] | ✅ `["2026-05-22", "2026-05-23"]` |
| references: 指向执行体 | ✅ `.claude/skills/concept-fable/SKILL.md` |

**正文章节对照**：

| §3.8 spec 章节 | skill-concept-fable.md |
|---|---|
| 引言 | ✅ |
| 设计意图 | ✅ |
| 实战 Pitfalls | ✅（"待首次实战后回填"占位 + 预期 4 类坑）|
| 演化简史 | ✅（v1.0 表）|
| 当前状态 | ✅（含执行体目录树）|
| 反思与未解决问题 | ✅（4 条待解）|
| References | ✅ |
| (可选)元注 | ✅（声明本页是 D9 testbed）|

## 6. concept-fable 三件套自洽 ✅

三个文件互相引用 + 描述的 skill 行为一致：

- **反思页**（skill-concept-fable.md）：References 指向 `.claude/skills/concept-fable/SKILL.md` + `references/prompt-template.md` ✅
- **执行体**（concept-fable-SKILL.md）：References 指向 prompt-template.md + skill-concept-fable.md 反思页 + design-principles + CLAUDE.md §3.9 ✅
- **prompt 模板**（prompt-template.md）：开头引用 `.claude/skills/concept-fable/SKILL.md` Step 2 ✅

边界分类（4 档：感觉型/方法论/结构型/拒绝）在三个文件里描述一致 ✅。

## 7. Dogfood 检查 ✅

v2.5 本身是否符合 v2.5 的原则？

- **减优于加**：v2.5 新增内容逐项审过，每条都必要（AI 心法 / 受众分层 / 反膨胀 / propagation 矩阵 / review 体检 / skillify 双重存在 / D9 模板 / 写作模板 / Timeline 归档 / B 组指标 / Claudian skills 段）。无"为完整性而加"的内容。✅
- **合并优于追加**：propagation 矩阵合并到 X 的 propagation 段；review 尺寸体检合并到 X 的 review 段；skillify 融合 X 的 Hermes 自动复盘 + Y 的双重存在。✅
- **删除优于保留**：删了 v2.4-X 过渡内容（"对已有页面的影响"段），删了 Changelog 旧版本的详细条目。✅
- **关键检查不能省**：v2.5 起草跑过 v2.4-Y 漏掉 v2.1/v2.2/v2.3 章节的发现 + 跟 vault 现有 skill-review-digest.md 范本对齐 + D9 spec 自洽性验证。✅

## 8. DEPLOY-v2.5.md 跟现状对齐 ✅（含 1 个 🟡）

- Phase 0 用户已跑，分支 v2.5-integration 创建成功，文件清单符合预期 ✅
- Phase 7 `.tmp-claude-reports/incoming-v2.4-Y/` 已不存在（用户已清），`rm -rf` 容忍不存在，无害 no-op ✅
- 🟡 验证清单「预期」行数偏高（见第 2 项），不阻塞

---

## 🟡 建议修订（3 项，非阻塞）

### 🟡 #1 受众分层表 design-principles vs CLAUDE.md 不一致

**问题**：

- **design-principles v2.5 第二层「受众分层」表第一行**：把 `skill 反思页` 列入「当前 AI」那层
- **CLAUDE-v3 §1.1 受众契约表**：隐含把 `wiki/pages/` 主体（含 skill 反思页文件 wiki/pages/skill-*.md）列入「未来的 Haopeng」层
- **同一物理文件**（wiki/pages/skill-*.md）在两份规则里属于不同受众层

**实际语义判断**：

按 vault 现有 `skill-review-digest.md` 范本，反思页内容（设计意图、Pitfalls、演化、反思）明显是给**未来 Haopeng**复盘 skill 演化用的——不是 AI 写入时的 spec。AI 在 skillify 时只是参考。主受众应该是未来 Haopeng。

**修订建议**：

design-principles v2.5 受众分层表第一行改为：

| 位置 | 受众 |
|---|---|
| `design-principles.md` + `wiki/CLAUDE.md` + `~/.hermes/skills/{name}/SKILL.md` + `.claude/skills/{name}/SKILL.md` | 当前 AI |
| `wiki/pages/` 主体（含 skill 反思页）| 未来的你 |

（删除原第一行末尾的「skill 反思页」）

### 🟡 #2 CLAUDE-v3 §3.6 Filename Convention 漏 MOC 命名约定

**问题**：

- **design-principles v2.5** 第三层导航明确："某 tag 下 ≥5 页时建 `{topic}-moc.md`（小写+连字符+后缀）"
- **CLAUDE-v3 §3.6** 列出了 Entity 页 / Concept 页 / Skill 反思页命名约定，**漏了 MOC 页**

**修订建议**：

CLAUDE-v3 §3.6 加一行：

```
- MOC 页：`{topic}-moc.md`（小写连字符 + `-moc` 后缀，如 `amazon-moc.md`、`people-moc.md`）
```

### 🟡 #3 CLAUDE-v3 §3.8 frontmatter 注释 + References 段只提了 `~/.hermes/skills/` 没提 `.claude/skills/`

**问题**：

CLAUDE-v3 §3.8 模板写的 references 字段值是 `~/.hermes/skills/{name}/SKILL.md`。但 skill-concept-fable.md 实际写的是 `.claude/skills/concept-fable/SKILL.md`——因为它的执行体在 Claudian 端而非 Hermes 端。

按 design-principles v2.5「Skill 系统设计」第 4 条「协议互通」，两个路径都合法（Hermes / Anthropic 互通），CLAUDE.md 应该说明 references 字段值跟执行体实际位置一致。

**修订建议**：

CLAUDE-v3 §3.8 frontmatter 注释改为：

```yaml
references:                # 指向执行体物理路径（~/.hermes/skills/ 或 .claude/skills/，跟执行体实际位置一致）
  - "~/.hermes/skills/{name}/SKILL.md"   # 或 ".claude/skills/{name}/SKILL.md"
  - "~/.hermes/skills/{name}/references/"
```

CLAUDE-v3 §3.8 References 段也加上 `.claude/skills/` 备选。

---

## 处置建议

3 个 🟡 都是「内部一致性 / spec 漏项」级别问题，不阻塞设计正确性，但部署后会留下"两份规则文件描述不齐"的隐患——未来扩展时容易再触发。

**两种处置策略**：

### 策略 A：现在修订（推荐）

我现在修订 `incoming-v2.5/design-principles-v2.5.md` + `CLAUDE-v3.md`，把 3 个 🟡 都解决。然后 Claude Code 继续 Phase 1.1 生成 diff（你的 diff 审会看到新增修订）。

**优点**：v2.5 一次性干净落地，未来不留小尾巴。
**成本**：增加约 3-5 行内容到两份规则文件，且让 Claude Code 等几分钟。

### 策略 B：先部署，后续 patch

直接让 Claude Code 继续 Phase 1，三个 🟡 留到 v2.5.1 或下次 review 时一起改。

**优点**：部署更快，v2.5 主体不再动。
**成本**：vault 里两份规则文件留 3 处轻度不一致，下次 distill / harvest / lint 时可能被 Claudian 注意到反复问。

**我的推荐**：**策略 A**。理由：

1. 3 个 🟡 都是规则层的措辞补全，**改的就在新写的内容里**——不涉及结构调整
2. 修订成本 < 5 分钟（我已经写好修订内容，直接 patch）
3. v2.5 是「整合 + 反熵」性质的版本，规则层小不一致跟 v2.5 自己的"机械式枚举 + 严格自检"心法矛盾——dogfood 不一致会让心法可信度打折

请回："A 修订" 或 "B 部署"。
