# concept-fable v2.6 回填 diff 草稿

> **来源**:`.tmp-claude-reports/concept-fable-dogfood.md` 238 行(陌生化 / JTBD / 库存周转率 三轮 dogfood)。
> **状态**:草稿,**未应用**。两个目标文件原样。等审过回「可以 apply」我才动。
> **应用方式**:用 Edit 工具按顺序执行,每条 OLD 用本文件「改前」原文,NEW 用「改后」。已确认所有 OLD verbatim 取自 read(中文标点没重新输入)。
>
> **改动总览**:
> - `prompt-template.md` 6 条(改动 1–6)
> - `skill-concept-fable.md` 2 条(改动 7–8)
> - **角色边界守住**:规则/约束/模板/默认行为全装 prompt-template.md;Pitfalls 表/设计意图/迭代记录全装 skill-concept-fable.md。
>
> **一处需你裁决的结构性决策(选项 a / b / c,详见上一条对话报告)**:本草稿按 **(a)** 走 ——`prompt-template.md` 的 `### A. 类型判断与警告` 重命名为 `### A. 适用边界`,新内容(光谱表 / #3 分档 / #1 --draft / #4 边界)都装在 A 里作 A.1–A.4 子节。若你坚持 (b)「保留 A 旧名,新内容仍装 A 内作 subsection」,告诉我我重写改动 1–4 的「改后」字段(改前不变)。

---

## 改动 1 — Section A 重命名 + A.1 三层光谱表

- **文件**:`.claude/skills/concept-fable/references/prompt-template.md`
- **位置**:L25(Section A 标题行)
- **对应**:结构选项 (a) 重命名 + 光谱表回填(dogfood.md L223 三层光谱表整张)
- **性质**:rename + 紧接插入

**改前**:

````text
### A. 类型判断与警告
````

**改后**:

````text
### A. 适用边界

#### A.1 三层光谱(选哪类概念用 fable)

三轮 dogfood(陌生化 / jobs-to-be-done / 库存周转率,2026-05-23)实测确认。详见 `.tmp-claude-reports/concept-fable-dogfood.md`「三层光谱」段。

| 概念档位 | SKILL 边界表 | 寓言能锁的部分 | 寓言锁不到的部分 |
|---|---|---|---|
| **感觉型 / 关系型** | 强烈推荐 | 核心感觉(陌生化:"看见熟悉的木勺") | 主动技法面(创作者如何刻意操作) |
| **方法论 / 边界模糊** | 可选 | 视角翻转感觉(JTBD:"问对问题") | 结构化方法学(forces / job statement 等) |
| **结构型** | 警告 | 极泛入门印象(库存周转:"流动好") | 公式、行业基准、trade-off、操作 |
````

---

## 改动 2 — A.2 概念类型分档默认行为(取代原结构型警告)

- **文件**:`.claude/skills/concept-fable/references/prompt-template.md`
- **位置**:L27–33(原结构型警告 callout 整段)
- **对应**:Pitfall #3「边界模糊概念默认问用户」+ 原警告归并入档
- **性质**:整段替换,原警告 callout 文字保留(下移到「警告档」子项里)

**改前**(L27–33,verbatim):

````text
如果概念类型 = 结构型（FIFO、漏斗转化率、库存周转率、布林带等明确结构/数值关系的概念）：

> ⚠️ "{concept}" 是结构型概念。寓言可以帮你"记得有这回事",但不能替代结构理解。强塞寓言可能掩盖关键的数值/层级/序列关系。
> 
> 仍要继续吗？如果是,本次产出的直觉锚点段落会附注"本段服务于记忆触发,不替代结构本身——结构请见下方"。

等待用户确认。
````

**改后**:

````text
#### A.2 概念类型分档默认行为

按 A.1 档位决定调用 fable 后的默认行为(production 模式;`--draft` 见 A.3):

- **强烈推荐档(感觉型 / 关系型)** → 直接进 Section B 生成 3 候选
- **可选档(方法论 / 边界模糊)** → **先问用户**:「你想要的是"记住感觉 / 视角翻转"还是"学结构"?」
  - 答"记忆 / 视角" → 进 Section B 生成
  - 答"结构" → 不出寓言,建议改用类比图 / cases / decision tree / method 笔记
- **警告档(结构型,如 FIFO、漏斗转化率、库存周转率、布林带)** → 显式确认 + 警告 callout:

> ⚠️ "{concept}" 是结构型概念。寓言可以帮你"记得有这回事",但不能替代结构理解。强塞寓言可能掩盖关键的数值/层级/序列关系。
>
> 仍要继续吗?如果是,本次产出按 B.5「结构型专用产出模板」生成(每候选附「记忆触发」blockquote + 末尾「本寓言不能回答的问题清单」+ 指向结构本身的外部来源)。

等待用户确认。
````

---

## 改动 3 — A.3 目标页缺失分支 + `--draft` 开关

- **文件**:`.claude/skills/concept-fable/references/prompt-template.md`
- **位置**:L35(原 `### B. 候选寓言生成（3 个）` 标题行 — 在它之前插入 A.3 整段)
- **对应**:Pitfall #1「目标页不存在时无 fallback → 显式 --draft」
- **性质**:纯插入(anchor 是 B 标题,保留不动)

**改前**:

````text
### B. 候选寓言生成（3 个）
````

**改后**:

````text
#### A.3 目标页缺失分支(`--draft`)

**默认 production 调用** `/concept-fable {page-name}`:若 `wiki/pages/{page-name}.md` 不存在 → **abort 并报错**「目标页不存在,无法读取 Compiled Truth 作输入」,不要自行延伸。

**`--draft` 开关** `/concept-fable {name} --draft`:dogfood / 探索性使用专用。
- 跳过 Step 1 的 wiki 页读取
- 用 LLM 对概念的常识知识作输入,**生成前明示用户「输入来自 LLM 常识,请审是否准确」**
- 跳过 Step 5(写入 wiki)/ Step 6(自检 wiki 页规范)/ Step 7(commit)
- 产出仅供评估,**不进 vault**

### B. 候选寓言生成（3 个）
````

---

## 改动 4 — A.4 寓言的边界不止概念类型,也在概念某一面

- **文件**:`.claude/skills/concept-fable/references/prompt-template.md`
- **位置**:L35(应用改动 3 后,锚点 `### B. 候选寓言生成（3 个）` 仍唯一存在;在其之前插入 A.4)
- **对应**:Pitfall #4「方法论类只覆盖感觉/翻转面,结构化方法学维度寓言不该碰」
- **性质**:纯插入

**改前**:

````text
### B. 候选寓言生成（3 个）
````

**改后**:

````text
#### A.4 寓言的边界不止概念类型,也在概念某一面

A.1 光谱按"概念档位"分,但同一档内某个概念可能有多面(感觉 / 视角翻转 / 结构化方法学 / 主动操作技法)。每候选寓言常常只锁住其中**一面**:

- **感觉型若兼具"主动技法"面**(例:陌生化的"创作者刻意构造陌生"):候选默认偏被动唤醒侧,技法面被推到「边界」段轻描淡写
- **方法论类**(JTBD / OKR / 5 Whys 等):寓言只能覆盖"视角翻转"面,**结构化方法学维度寓言不该碰** —— 强塞会模糊框架精度

→ Section B 生成前检查目标概念是否兼有"感觉/视角 + 操作/结构"两面;若是,产出末尾必须明示「本组寓言聚焦 X 面,不替代 Y 面学习」,并把 Y 面的学习路径(类比图 / cases / 公式 / 教材)告诉用户。

### B. 候选寓言生成（3 个）
````

---

## 改动 5 — B 约束段加「操作面候选要求」

- **文件**:`.claude/skills/concept-fable/references/prompt-template.md`
- **位置**:L46(原约束列表末行 `- 3 个候选应来自**不同的生活场景类型**——避免都用职场、都用家庭、都用学习`)
- **对应**:Pitfall #2「感觉型若兼具主动技法,候选都偏被动唤醒」
- **性质**:在约束列表末尾追加 1 条 bullet

**改前**(L46):

````text
- 3 个候选应来自**不同的生活场景类型**——避免都用职场、都用家庭、都用学习
````

**改后**:

````text
- 3 个候选应来自**不同的生活场景类型**——避免都用职场、都用家庭、都用学习
- **若概念兼有"感觉/视角 + 操作/技法"两面**(参 A.4):3 候选中至少 1 个聚焦**操作面**(行动者主动做了什么),不能 3 个都是"外部 disruption 让人重看"的被动结构
````

---

## 改动 6 — B.5 结构型专用产出模板(三件套)

- **文件**:`.claude/skills/concept-fable/references/prompt-template.md`
- **位置**:L62(原 B「输出结构」代码块结束行 ` ``` `)+ L63 空行 + L64(`### C. 检验问题的硬要求`)—— 在 C 标题之前插入 B.5 整段
- **对应**:Pitfall #5(每候选附「记忆触发」blockquote)+ Pitfall #6(SKILL 没规定结构型继续后产物格式)
- **性质**:纯插入(anchor 是 C 标题,保留不动)

**改前**:

````text
### C. 检验问题的硬要求
````

**改后**:

````text
### B.5 结构型专用产出模板(警告档继续后强制)

A.2 警告档用户确认继续后,产物**必须**额外满足三件套(SKILL 现状只有警告 callout,不规定继续后产物格式;dogfood 三次都自发加但 LLM 的好心不可靠,本节强制):

**① 每候选寓言尾部必须附**:

> ⚠️ **本段服务于记忆触发,不替代结构本身。** 真正的 {concept} 的[公式/数值结构/行业基准/trade-off]在 [指向结构来源],寓言不展示。

**② 候选末尾追加「本寓言不能回答的问题清单」**(至少 2 条):

```
本寓言不能回答的问题(请另查结构本身):
- (具体问题 1,如"公司库存周转率 8 是好是坏")
- (具体问题 2,如"如何选 ABC 分类阈值")
```

**③ 整个产出末尾追加「结构本身的外部来源」清单**(让用户知道去哪学结构):

```
学结构本身请见:
- 教材章节(如《会计学原理》第 N 章)
- 行业基准(如 [行业] 同业财报、行业报告)
- 公式参考(如 Investopedia / 课程笔记 / 公司内部 wiki)
```

### C. 检验问题的硬要求
````

---

## 改动 7 — 设计意图段加「适用范围(三轮 dogfood 后)」洞察段

- **文件**:`wiki/pages/skill-concept-fable.md`
- **位置**:L29(原「**核心痛点**:…故事是大脑愿意主动复述的载体。」段)+ L31(原「**为什么落在 Claudian 而不是 Hermes**…」段)之间插入新段
- **对应**:洞察「fable 是工具链一环,只负责感觉/翻转那一段」
- **性质**:纯插入(anchor 是核心痛点段末尾)

**改前**(L29,verbatim):

````text
**核心痛点**：分享完概念后听者点头说懂、转头就忘。这种问题加大讲解力度解决不了，因为问题不在讲的人不努力，而在大脑对抽象概念的留存机制天然偏弱。故事是大脑愿意主动复述的载体。
````

**改后**:

````text
**核心痛点**：分享完概念后听者点头说懂、转头就忘。这种问题加大讲解力度解决不了，因为问题不在讲的人不努力，而在大脑对抽象概念的留存机制天然偏弱。故事是大脑愿意主动复述的载体。

**适用范围(三轮 dogfood 后的修订,2026-05-23)**:fable 不是"学懂概念"的全套工具,而是**工具链一环,只负责概念的"感觉 / 视角翻转"那一段**。感觉型概念能由 fable 单点完成(陌生化:"看见熟悉的木勺");方法论类靠 fable + 类比 + cases 组合(fable 锁视角,类比 / cases 教结构);结构型靠 fable + 教材 / 行业数据(fable 给"有这回事"印象,结构必须从公式 / 基准另学)。详见 `.claude/skills/concept-fable/references/prompt-template.md` A.1 三层光谱表。
````

---

## 改动 8 — 实战 Pitfalls 段:placeholder 整段重写为「按档分组 + 6 条 Pitfall 全集」

- **文件**:`wiki/pages/skill-concept-fable.md`
- **位置**:L35–44(原「## 实战 Pitfalls」section 整段:placeholder 文字 + 预期方向列表 + 格式说明)
- **对应**:Pitfall #1–#6 全集回填(分档对应 SKILL 边界表三档)
- **性质**:整段替换

**改前**(L35–44,verbatim):

````text
## 实战 Pitfalls

待首次实战后回填。预期会在以下几个方向遇到坑：

- 寓言生成时陷入"嵌套概念解释概念"（用一个抽象概念解释另一个抽象概念，背离用日常场景包裹的初衷）
- 检验问题写成"复述比喻"（如"老王为什么走小巷？"）而非"概念迁移"
- 结构型概念触发警告后用户选择继续，结果产出确实掩盖了关键结构
- 直觉锚点段落超过 100 行 soft limit

每条踩坑发生后按照 `wiki/pages/skill-review-digest.md` 的 Pitfalls 表格式补：Pitfall / 性质（工具调用约束 / 环境约束 / 平台约束 / 业务语义校准 / 平台陷阱）/ 应对。
````

**改后**:

````text
## 实战 Pitfalls

三轮 dogfood(陌生化 / jobs-to-be-done / 库存周转率,2026-05-23)沉淀。按 SKILL 边界表三档分组——每档暴露的 Pitfall 模式不同。原始归档见 `.tmp-claude-reports/concept-fable-dogfood.md`。

### 感觉型档(强烈推荐)

| Pitfall | 性质 | 应对 |
|---|---|---|
| **#1 目标页不存在时 SKILL / prompt template 无 fallback,dogfood / 探索性使用没法跑** | 流程缺陷 | prompt-template.md A.3 加显式 `--draft` 标志:跳 Step 1 页读取,用 LLM 常识 + 用户验证作输入,跳 Step 5/6/7;产出仅供评估 |
| **#2 感觉型若兼具"主动技法"维度,寓言会偏被动唤醒,技法面只在"边界"提一句没正面给** | 生成偏差 | prompt-template.md B 约束加:概念若兼有"感觉 + 操作"两面,候选中至少 1 个聚焦操作面 |

### 边界模糊档(可选 / 方法论)

| Pitfall | 性质 | 应对 |
|---|---|---|
| **#3 边界模糊概念 SKILL 写「问 Haopeng」,dogfood / 自动化调用里这条规则没自动触发** | 流程不严 | prompt-template.md A.2 分档默认行为:可选档默认先问「记忆 vs 结构?」根据回答决定 generate / 改推荐别的载体 |
| **#4 方法论类概念寓言只覆盖视角翻转面,结构化方法学维度寓言不该碰** | 适用边界本质 | prompt-template.md A.4 加补注:概念某一面也是边界,产出末尾明示「本组寓言聚焦 X 面,不替代 Y 面学习」 |

### 结构型档(警告)

| Pitfall | 性质 | 应对 |
|---|---|---|
| **#5 结构型寓言反复需要附"不替代结构本身"注,SKILL 现状只在 prompt template A 段一段警告,生成时是否每候选加靠 LLM 记得** | 生成一致性缺陷 | prompt-template.md B.5 结构型专用产出模板:强制每候选附「记忆触发」blockquote + 候选末尾「本寓言不能回答的问题清单」 |
| **#6 SKILL 对结构型只「警告 + 确认」,没规定继续后产物的格式应有什么不同;dogfood 三次都自发加了三件套,LLM 的好心不可靠** | 流程不严 | prompt-template.md B.5 三件套强制:① 每候选「记忆触发」blockquote ② 末尾「不能回答的问题清单」 ③ 产出末尾指向"结构本身"的外部来源(教材 / 行业报告 / 公式参考) |

每条新踩坑发生后,按照 `wiki/pages/skill-review-digest.md` 的 Pitfalls 表格式补到对应档:Pitfall / 性质(工具调用约束 / 环境约束 / 平台约束 / 业务语义校准 / 平台陷阱 / 流程缺陷 / 生成偏差 / 生成一致性缺陷 / 适用边界本质)/ 应对。
````

---

## 应用前自检清单(给主对话 / Claude Code 用)

应用 diff 之前确认:

- [ ] 选项 (a) 重命名 Section A 已认可(若选 (b) 我重做改动 1–4)
- [ ] 每条改动的「改前」与目标文件当前内容完全一致(应用 Edit 时 OLD 字段直接用本文件「改前」原文)
- [ ] 应用顺序:**改动 1 → 2 → 3 → 4 → 5 → 6 → 7 → 8**(顺序敏感:改动 3 和 4 都用 L35 `### B. 候选寓言生成（3 个）` 作 anchor,改动 3 在前)
- [ ] 应用完跑 wc -l 验证:
  - `prompt-template.md` 预计 144 + ~70 = **~214 行**
  - `skill-concept-fable.md` 预计 108 + ~25 = **~133 行**
- [ ] 不 commit,留主对话审过 vault diff 后再决定 commit 时机
